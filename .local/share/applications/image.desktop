# All image/* files are opened with `nsxiv %f`, which is not suitable
# for image/gif because it requires `nsxiv -a %f`. This file fixes that.

[Desktop Entry]
Type=Application
Name=Image
Exec=/usr/local/bin/nsxiv -a %f
